# 参与贡献

本项目按 [Apache-2.0](LICENSE) 发布。按该许可证第 5 条，你有意提交的贡献默认以同一许可证授出，
**不需要单独签 CLA**。

## 克隆后第一件事：装 pre-push 钩子

```bash
ln -sf ../../hooks/pre-push .git/hooks/pre-push
```

CI 会跑同样的检查，但 CI 在 push **之后**才运行——对防泄露而言已经晚了。本钩子是唯一能在内容
公开前拦住的一道。

## 三条硬规则

1. **不得提交任何真实表单数据。** 表单 URL、字段 ID、下拉枚举、项目名称、周报内容、截图和
   登录态一律只存于每用户私有的 `$WORK`。公开仓只放通用代码和空白模板。
   `tests/test_public_tree.py` 与 `tests/scan_history.py` 会拦，但别依赖它们兜底——
   连测试里的假数据都要避开真实字段 ID 的形状（这条拦过本项目自己写的测试）。
2. **提交身份必须是 GitHub noreply 地址。** 真实邮箱（尤其公司域名）不得进入公开历史。
   ```bash
   git config --local user.email "<你的 GitHub 数字 ID>+<用户名>@users.noreply.github.com"
   ```
   钩子会校验，忘了设会被拦下。
3. **PR 必须通过 CI 三个 job**：单元测试、仿真表单 e2e、历史脱敏扫描。

## 提交前

按改动类型选择必跑的验证，见 [docs/SOP.md](docs/SOP.md#改动类型--必跑验证)。任何改动至少要过
pre-push 钩子。

改代码之前先读 [docs/REVIEW.md](docs/REVIEW.md) 第二节的**不变量清单**——那些是本项目刻意
维持的设计约束（代码与运行数据分离、组织私有面全配置化、安全边界落在确定性层而非提示词里）。
如果你的改动要打破其中一条，请在 PR 里说明替代方案为什么同样成立。

## 不接受的改动

- 给「只落草稿、永不提交」加旁路——工具**没有**提交能力是设计，不是遗漏。
- 用命令行开关绕过校验（如让附件校验可跳过）。组织差异走配置，不走开关；
  开关会变成「失败了就加参数绕过」的逃生口。
- 静默兜底：捕获异常后继续、定长 sleep 假定成功、把配置错误当成「没配置」。
  本项目一律 fail-loud。
