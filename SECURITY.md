# 安全说明与信任模型

本技能在 skills.sh 上带有 Socket `WARN` 与 Snyk `WARN` 两个标识。**这两条都准确描述了本工具的
固有能力，不是缺陷。** 本文说明它们是什么、已有哪些约束、以及什么情况下你不该装这个工具。

## 这个工具能做什么

它用 Playwright 驱动一个无头 Chromium，登录你所在组织的氚云（H3yun）表单，把你**已经人工审核
过**的周报内容填进去并**存为草稿**。仅此而已。

不变量（由代码强制，不是靠文档自觉）：

- **没有提交能力。** 脚本里不存在点「提交」的代码路径；`form_texts.save_draft` 若被配成提交
  按钮，配置校验直接拒绝。最终提交永远由你在钉钉里手动完成。
- **落草稿必须显式双标志** `--draft --confirmed`，且 `--confirmed` 在文档里明确标注为
  「操作者声明」，不是人审证据。
- **表单地址被域名硬限制**：`validate_form_url()` 只接受 `https://` 且主机为 `h3yun.com` 或
  `*.h3yun.com`；`file://` 仅用于本地仿真测试。一次性登录链接另需满足 `*.h3yun.com` +
  `/entry/auth` + 含 token，且**禁止被存成 `form_url`**。
- **登录链接不经过 agent**：只接受你本人在交互终端的隐藏输入（`getpass`），非 TTY 直接退出，
  拒绝从命令行参数读取。
- **不读取你的仓库**：工作日志只解析一个固定路径的 Markdown 文件，不扫描代码、不读 git log。
- **不代你 push**：若你的工作目录是 git 仓库，技能只 `commit`，推送到远端由你自己执行。

## 扫描器标了什么，为什么

### Snyk W012 — Unverifiable external dependency（MEDIUM）

> 运行时经 Playwright `page.goto` 访问用户提供的 h3yun 表单 URL，会执行远端 Web 应用代码，
> 远端内容可影响执行。

**属实，且无法消除。** 任何"自动填网页表单"的工具都必须加载那个网页。可以说明的是：

- URL 不是任意的，被硬限制到单一厂商域名（见上）。
- 脚本**不执行页面提供的代码**：只做点击、填值、读取文本与可见性、截图。没有 `eval`，
  没有把页面内容当代码跑。
- 页面内容能影响的最坏结果是"填错或填不进去"，而**填错也只会停在草稿**，仍需你人工核对后
  才可能进入正式申报。

唯一的彻底消除路径是改用氚云 OpenApi 取代浏览器自动化（路线图 P3），目前受阻于拿不到
`EngineSecret`。

### Socket — Anomaly on SKILL.md（LOW）

> 核心能力与"半自动钉钉周报草稿"目的基本一致，未见明显凭证窃取或无关数据外传，整体不像恶意
> 技能。但安装依赖第三方 skills CLI 的转移信任链、自动化网页登录、以及可选 git push 使其具备
> 中等安全风险。

三条依据分别看：

| 依据 | 状态 |
|---|---|
| skills CLI 的转移信任链 | 分发渠道特性，不由本项目决定。可用 zip + `install.sh` 绕开，见 README |
| 自动化网页登录 | 固有。登录态存 `~/.config/dtwr/state.json`（0600），共享机上有属主校验 |
| 可选 `git push` | **已移除**：`$WORK` 是 git 仓库时只 `commit`，push 由用户自行执行 |

## 数据边界

| 数据 | 去向 |
|---|---|
| 周报内容、工时 | 只发往**你自己配置的**氚云表单 |
| 登录态 | 只存本机 `~/.config/dtwr/state.json`，0600，不上传任何地方 |
| 表单 URL、字段 ID、枚举、项目名 | 只存本机私有 `$WORK/config.json`，公开仓只有键名 |
| 工作日志 | 只读一个固定路径的本地 Markdown |
| 遥测 | **无**。本项目不发送任何使用数据 |

代码中出现的外部主机只有三个：`github.com`（README 链接）、`docs.astral.sh`（报错提示里的
安装文档链接）、`schemas.openxmlformats.org`（xlsx 的 XML 命名空间，不发起请求）。
氚云域名不在代码里硬编码，来自你的私有配置。

## 你不该装它的情况

- 你所在组织禁止用自动化手段操作 HR 系统——先获得许可。
- 你无法接受一个本地进程持有你的钉钉/氚云会话。
- 你期望它替你决定填什么。它不做这件事：内容必须你审，提交必须你点。

## 报告安全问题

请通过仓库 issue 报告，不要在 issue 中附带任何真实表单 URL、字段 ID、组织项目名称或登录链接
——那些是你的私有数据，本项目不需要它们也能定位问题。
